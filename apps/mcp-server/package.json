{
  "name": "@aimetric/mcp-server",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "dependencies": {
    "@aimetric/rule-engine": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit"
  }
}
