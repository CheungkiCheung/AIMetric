{
  "name": "@aimetric/metric-platform",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@aimetric/event-schema": "workspace:*",
    "@aimetric/git-attribution": "workspace:*",
    "@aimetric/metric-core": "workspace:*"
  }
}
